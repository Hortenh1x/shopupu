package com.example.shopupu.identity.entity;

/** A user's self-selected gender (optional, USER-06). Distinct from the catalog's
 * product {@code Gender} (MEN/WOMEN/UNISEX/KIDS) — this is a personal attribute. */
public enum Gender {
    MALE,
    FEMALE,
    OTHER
}
